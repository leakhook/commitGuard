{
  "name": "envguard",
  "version": "0.1.0",
  "description": "시크릿 커밋을 pre-commit 단계에서 막는 제로 설정 CLI",
  "type": "module",
  "bin": { "envguard": "dist/cli.js" },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "tsx --test test/*.test.ts",
    "prepublishOnly": "npm run build"
  },
  "engines": { "node": ">=18" },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsx": "^4.16.0",
    "@types/node": "^20.14.0"
  }
}
